# Delivery App

Instalar modulos:

```javascript
npm install
```

Copiar archivos de example.env a .env

```javascript
cp example.env .env
```

Correr el servidor:

```javascript
npm run start:dev
```
